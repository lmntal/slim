%  bubble.lmn
%  bubble? sort
%  昇順に並ぶ0～Nまでの要素を降順に整列する(バブルソートの最悪ケース)

% n(7) -> bubble_n7.il
% n(8) -> bubble_n8.il
% ...

% property0: 隣り合う2要素の大小関係が 全て">="である状態は存在しない
%           => 否定をとるため, 事実上は整列済みの状態への到達性(safety)検証となる

data_generator{
  n(5).
  data([0]).
  genN @@ n(N), data=[X |F] :- N > 0, N2=N-1 | n(N2), data=[X,N |F].
}.

init @@ data_generator{n(0), $p,@p}/ :- $p.
sort @@	L=[X,Y |R]	:- X<Y | L=[Y,X |R].
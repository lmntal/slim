# Library files - these are typically data/config files
# Install library files to appropriate location

# Create lib directory in install
install(DIRECTORY DESTINATION lib/slim)

# If there are specific library files, add them here
# install(FILES ... DESTINATION lib/slim)
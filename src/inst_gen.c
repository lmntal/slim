#include "instruction.h"


int main() {
  int i;

  for (i = 0; i < sizeof(spec)/sizeof(spec[0]); i++) {
    InstrSpec s = spec[i];
"$out_2"             { return OUTSIDE_PROXY; }
 printf("\"%s\" { return TK_%s; }\n", s.op_str, s.op_str

  }

}
